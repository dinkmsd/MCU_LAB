/*
 * fsm_manual.c
 *
 *  Created on: Oct 7, 2022
 *      Author: xanlo
 */

#include "fsm_manual.h"


void fsm_manual_run() {
	switch (status) {
	case MAN_RED:
		// TODO
		if (timer1_flag == 1) {
			status = AUTO_RED;
			setTimer1(5000);
		}
		break;

	case AUTO_RED:
		if (set1_flag == 1) {

		}
		break;
	case MAN_GREEN:
		if (timer1_flag == 1) {
			status = AUTO_RED;
			setTimer1(5000);
		}
		break;
	case MAN_YELLOW:

		break;
	}
}

